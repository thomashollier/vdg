
#---  push back local changes to main
git push -u origin main

#--- update local repo with online changes
git pull origin

#--- update single file with repo version
git fetch --all
git checkout origin/main -- stabFromTrack_v02.py




